// Intentionally left as a no-op.
// DraggableQuestionList uses Blazor drag events (@ondragover:preventDefault, @ondrop:preventDefault),
// so this component-scoped JavaScript file must not attach duplicate DOM listeners.
